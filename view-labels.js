// Printable label sheets: human-readable (code, name, location path) —
// no QR, per the iOS separate-storage problem (BLUEPRINT.md §13.3). Built
// in Phase 3, via report.js's print builder.
export {};
