// PURE merge logic — no DOM, no network, no storage. Built in Phase 4, first
// and fully unit-tested before sync.js is written against it.
// resolveRecord(a, b), mergeItemSets(local, remote), computeMediaActions(...).
export {};
