// { designer, date, build } from version.js as one line. Currently rendered
// directly by app.js (boot-critical, needed from Phase 0); this module stays
// a placeholder in case About grows beyond one line later.
export {};
