// Network-only: calls the Anthropic API directly from the browser to draft
// container contents from photos. Built in Phase 2. Imports aiplan.js for the
// pure prompt/parse/estimate logic; owns nothing but the fetch calls.
export {};
