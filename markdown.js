// PURE minimal Markdown renderer for help.js content. One source line per
// paragraph/bullet (a line-based renderer turns a bare newline into <br>).
// Must escape raw HTML and reject javascript: URLs — tested in Phase 3.
export {};
