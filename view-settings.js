// Platform settings beyond theme/language (which app.js owns directly):
// density, storage estimate, sync now/backup/restore, danger zone. Theme and
// language pickers are wired by app.js since they're needed from Phase 0.
// Built out in Phase 3/4.
export {};
